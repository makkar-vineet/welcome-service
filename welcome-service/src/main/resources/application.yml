spring:
  application:
    name: welcome-service

  boot:
    admin:
      client:
        url: http://localhost:8181

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
